// <auto-generated />
#nullable enable
using System;
using System.Data;
using System.Text;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Data.SqlClient;
using AdoGen.SqlServer;

namespace AdoGen.Generator.Tests;

public sealed partial record VersionedOrder : ISqlSingleIdModel<global::AdoGen.Generator.Tests.VersionedOrder, Guid>
{
    private const string SqlDeleteBatchPrefix = "DELETE t FROM [dbo].[VersionedOrders] AS t JOIN (VALUES ";
    private const string SqlDeleteBatchSuffix = ") AS ids([Id]) ON ids.[Id]=t.[Id]";
    private const string SqlExists = "SELECT TOP(1) 1 FROM [dbo].[VersionedOrders] WHERE [Id] = @Id;";

    public static async ValueTask<int> DeleteAsync(SqlConnection connection, List<Guid> ids, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (ids is null || ids.Count == 0) return 0;
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);
        
        await using var cmd = new SqlCommand("", connection, transaction);
        if (commandTimeout.HasValue) cmd.CommandTimeout = commandTimeout.Value;
        cmd.EnableOptimizedParameterBinding = ids.Count > 24;
        
        var sb = new StringBuilder(SqlDeleteBatchPrefix);
        for (var i = 0; i < ids.Count; i++)
        {
            if (i > 0) sb.Append(',');
            sb.Append($"(@p{i})");
            cmd.Parameters.Add(VersionedOrderSql.CreateParameterId(ids[i], $"@p{i}"));
        }
        sb.Append(SqlDeleteBatchSuffix);
        cmd.CommandText = sb.ToString();
        
        return await cmd.ExecuteNonQueryAsync(ct).ConfigureAwait(false);
    }

    public static async ValueTask<bool> ExistsAsync(SqlConnection connection, Guid id, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);
        await using var cmd = connection.CreateCommand(SqlExists, CommandType.Text, transaction, commandTimeout);
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterId(id));
        var scalar = await cmd.ExecuteScalarAsync(ct).ConfigureAwait(false);
        return scalar is not null && scalar is not DBNull;
    }
}

public sealed partial record VersionedOrder : ISqlDomainModel<global::AdoGen.Generator.Tests.VersionedOrder>
{
    private const string SqlCreateTable = 
        """
        CREATE TABLE [dbo].[VersionedOrders](
            [Id] UNIQUEIDENTIFIER DEFAULT NEWID() NOT NULL,
            [ProductName] VARCHAR(50) NOT NULL,
            [Version] INT NOT NULL
        ,CONSTRAINT [PK_VersionedOrders] PRIMARY KEY ([Id]));
        """;
    private const string SqlInsert = "INSERT INTO [dbo].[VersionedOrders] ([Id], [ProductName], [Version]) VALUES (@Id, @ProductName, @Version);";
    private const string SqlInsertBatchTemplate = "INSERT INTO [dbo].[VersionedOrders] ([Id], [ProductName], [Version]) VALUES";
    private const string SqlUpdate = "UPDATE [dbo].[VersionedOrders] SET [ProductName] = @ProductName, [Version] = @Version + 1 WHERE [Id] = @Id AND [Version] = @Version;";
    private const string SqlDelete = "DELETE FROM [dbo].[VersionedOrders] WHERE [Id] = @Id AND [Version] = @Version;";
    private const string SqlUpsert = "UPDATE [dbo].[VersionedOrders] SET [ProductName] = @ProductName WHERE [Id] = @Id; IF @@ROWCOUNT = 0 INSERT INTO [dbo].[VersionedOrders] ([Id], [ProductName], [Version]) VALUES (@Id, @ProductName, @Version);";
    private const string SqlTruncate = "TRUNCATE TABLE [dbo].[VersionedOrders];";

    public static async ValueTask CreateTableAsync(SqlConnection connection, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);
        await using var cmd = connection.CreateCommand(SqlCreateTable, CommandType.Text, transaction, commandTimeout);
        await cmd.ExecuteNonQueryAsync(ct).ConfigureAwait(false);
    }

    public static async ValueTask<int> InsertAsync(global::AdoGen.Generator.Tests.VersionedOrder model, SqlConnection connection, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);
        await using var cmd = connection.CreateCommand(SqlInsert, CommandType.Text, transaction, commandTimeout);
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterId(model.Id));
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterProductName(model.ProductName));
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterVersion(model.Version));

        return await cmd.ExecuteNonQueryAsync(ct).ConfigureAwait(false);
    }

    /// <summary>
    /// Inserts multiple database records in one roundtrip. 
    /// Will throw if parameter count exceeds SQL Server limit (2100).
    /// For type global::AdoGen.Generator.Tests.VersionedOrder, each record will use 3 parameters.
    /// Resulting in a max insert count of 700 per batch.
    /// For larger inserts, consider using SqlBulkCopy or multiple batches.
    /// </summary>
    /// <param name="models"></param>
    /// <param name="connection"></param>
    /// <param name="ct"></param>
    /// <param name="transaction"></param>
    /// <param name="commandTimeout"></param>
    /// <returns>Number of affected rows</returns>
    public static async ValueTask<int> InsertAsync(List<global::AdoGen.Generator.Tests.VersionedOrder> models, SqlConnection connection, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (models is null || models.Count == 0) return 0;
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);

        await using var cmd = new SqlCommand("", connection, transaction);
        if (commandTimeout.HasValue) cmd.CommandTimeout = commandTimeout.Value;
        cmd.EnableOptimizedParameterBinding = (models.Count * 3) > 24;

        var sb = new StringBuilder(SqlInsertBatchTemplate.Length + models.Count * 17);
        sb.Append(SqlInsertBatchTemplate);

        var paramIndex = 0;

        for (var modelIndex = 0; modelIndex < models.Count; modelIndex++)
        {
            if (modelIndex > 0) sb.Append(',');
            sb.Append($"(@p{paramIndex}");
            cmd.Parameters.Add(VersionedOrderSql.CreateParameterId(models[modelIndex].Id, $"@p{paramIndex}"));
            paramIndex++;
            sb.Append($",@p{paramIndex}");
            cmd.Parameters.Add(VersionedOrderSql.CreateParameterProductName(models[modelIndex].ProductName, $"@p{paramIndex}"));
            paramIndex++;
            sb.Append($",@p{paramIndex})");
            cmd.Parameters.Add(VersionedOrderSql.CreateParameterVersion(models[modelIndex].Version, $"@p{paramIndex}"));
            paramIndex++;
        }

        cmd.CommandText = sb.ToString();
        return await cmd.ExecuteNonQueryAsync(ct).ConfigureAwait(false);
    }

    public static async ValueTask<int> UpdateAsync(global::AdoGen.Generator.Tests.VersionedOrder model, SqlConnection connection, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);
        await using var cmd = connection.CreateCommand(SqlUpdate, CommandType.Text, transaction, commandTimeout);
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterProductName(model.ProductName));
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterId(model.Id));
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterVersion(model.Version));

        var affected = await cmd.ExecuteNonQueryAsync(ct).ConfigureAwait(false);
                if (affected == 0) throw new global::AdoGen.SqlServer.AdoGenConcurrencyException("dbo.VersionedOrders");
                return affected;
    }

    public static async ValueTask<int> DeleteAsync(global::AdoGen.Generator.Tests.VersionedOrder model, SqlConnection connection, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);
        await using var cmd = connection.CreateCommand(SqlDelete, CommandType.Text, transaction, commandTimeout);
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterId(model.Id));
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterVersion(model.Version));

        var affected = await cmd.ExecuteNonQueryAsync(ct).ConfigureAwait(false);
                if (affected == 0) throw new global::AdoGen.SqlServer.AdoGenConcurrencyException("dbo.VersionedOrders");
                return affected;
    }

    public static async ValueTask<int> UpsertAsync(global::AdoGen.Generator.Tests.VersionedOrder model, SqlConnection connection, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);
        await using var cmd = connection.CreateCommand(SqlUpsert, CommandType.Text, transaction, commandTimeout);
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterId(model.Id));
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterProductName(model.ProductName));
        cmd.Parameters.Add(VersionedOrderSql.CreateParameterVersion(model.Version));

        return await cmd.ExecuteNonQueryAsync(ct).ConfigureAwait(false);
    }

    public static async ValueTask<int> TruncateAsync(SqlConnection connection, CancellationToken ct, SqlTransaction? transaction = null, int? commandTimeout = null)
    {
        if (connection.State != ConnectionState.Open) await connection.OpenAsync(ct).ConfigureAwait(false);
        await using var cmd = connection.CreateCommand(SqlTruncate, CommandType.Text, transaction, commandTimeout);
        return await cmd.ExecuteNonQueryAsync(ct).ConfigureAwait(false);
    }
}